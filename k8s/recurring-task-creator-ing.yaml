apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: recurring-task-creator
  namespace: '{{ "NAMESPACE" | env }}'
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
spec:
  ingressClassName: 'traefik'
  rules:
    - host: 'recurring-task-creator.{{ "NAMESPACE" | env }}.quant.benjamin-borbe.de'
      http:
        paths:
          - backend:
              service:
                name: recurring-task-creator
                port:
                  name: http
            path: /
            pathType: Prefix
  tls:
    - secretName: tls-quant
      hosts:
        - 'recurring-task-creator.{{ "NAMESPACE" | env }}.quant.benjamin-borbe.de'
