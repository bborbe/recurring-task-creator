apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: 'recurring-task-creator-{{ "NAMESPACE" | env }}'
rules:
  - apiGroups:
      - apiextensions.k8s.io
    resources:
      - customresourcedefinitions
    verbs:
      - get
      - create
      - update
      - patch
