apiVersion: kafka.strimzi.io/v1beta2
kind: KafkaUser
metadata:
  name: '{{ "NAMESPACE" | env }}-recurring-task-creator'
  namespace: strimzi
  labels:
    strimzi.io/cluster: my-cluster
spec:
  authentication:
    type: tls
